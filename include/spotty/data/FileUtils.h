/**
 * \file FileUtils.h
 * \brief Мелкие файловые операции, общие для ядра и плагинов.
 */
#pragma once

#include <spotty/api/SpottyApiExport.h>

#include <QString>

namespace spotty {

/**
 * \brief Создать каталог вместе с родительскими.
 * \param path Полный путь.
 * \return `true` при успехе.
 *
 * При неудаче пишет предупреждение в категорию `spotty.files`. Вызывающая сторона вправе
 * продолжить работу с урезанной функциональностью, а не завершаться: невозможность
 * создать каталог логов не повод не показывать данные с порта.
 *
 * \note Раньше жила в spotty::Paths. Там она оказалась по соседству, а не по смыслу: сам
 *       класс разрешает пути по политике приложения (переносной режим, каталог
 *       конфигурации), тогда как это — обёртка над `QDir::mkpath()` с диагностикой, и
 *       нужна она всем, кто пишет файлы, включая плагины, которым до spotty-core дороги
 *       нет.
 */
SPOTTY_API_EXPORT bool ensureDir(const QString &path);

} // namespace spotty
